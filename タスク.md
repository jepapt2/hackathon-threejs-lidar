## タスクチェックリスト

### 1. 環境 / 基盤セットアップ
- [✅] Node / pnpm(or npm) バージョン確認 & `.nvmrc` 作成
- [✅] Vite + React + TypeScript 初期化
- [✅] three.js / @react-three/fiber / @react-three/dreiインストール
- [ ] 最小シーン: Canvas / OrbitControls / 環境光 & 方向光
- [ ] サンプル GLB を `public/models/sample.glb` に配置
- [ ] GLB ローダ (useLoader(GLTFLoader, ...)) 動作確認
- [ ] リポジトリ初期コミット & main ブランチ保護(任意)

### 2. バックエンド方式確定と初期構築
- [ ] 要件比較: Amplify(GraphQL) vs Hono(REST) ドキュメントメモ作成
- [ ] 採用方式決定を README に明記
- [ ] Hono 選択時: `server/app.ts` 型エラー解消 (@types/node 追加など)
- [ ] Amplify 選択時: `amplify init` 実行
- [ ] 最小 API: /api/ping /api/models /api/pins /api/measurements (GET/POST)
- [ ] CORS 設定 (フロント localhost:5173 許可)

### 3. 3D モデルパイプライン整備
- [ ] iPad スキャン (Scaniverse) から GLB/PLY 取得手順を README に追記
- [ ] 不要メッシュ/ノイズ除去 (Blender で簡易クリーニング) 手順メモ
- [ ] Draco 圧縮 (gltf-transform など) スクリプト雛形 `scripts/compress-model.ts`
- [ ] S3 署名付き URL 取得 (Amplify or 手動 Lambda) / ローカル fallback 方針決定
- [ ] three.js Loader (GLTFLoader / PLYLoader) ラッパ util 作成 `src/lib/loadModel.ts`
- [ ] ロード時のエラーハンドリング & ローディング UI

### 4. 基本機能実装
- [ ] モデル表示 & 視点操作 (OrbitControls)
- [ ] ピン: zustand ストア (id, position, comment)
- [ ] ピン追加: クリック座標 → Raycaster → 交差点から設置
- [ ] ピン表示コンポーネント (Sprite or HTML overlay)
- [ ] ピン CRUD API 連携 (保存/取得)
- [ ] 計測: 2 点選択 → ライン表示 → 距離計算 (単位 m 表示)
- [ ] オブジェクト挿入: プリセット (human.glb, chair.glb) 選択→配置
- [ ] オブジェクト transform 編集 (ドラッグ or GUI)

### 5. 差分比較機能
- [ ] 過去モデル / 現在モデル 同一シーン読み込み
- [ ] サンプリング or 簡易メッシュ頂点距離計算アルゴリズム試作
- [ ] 閾値 (threshold) による色分け材質生成
- [ ] UI: Diff モード ON/OFF トグル
- [ ] UI: 閾値スライダ + 凡例 (色 → 距離範囲)
- [ ] パフォーマンス計測 (処理時間, メモリ) メモ化方式検討

### 6. パフォーマンス最適化
- [ ] Draco 圧縮済みモデル読み込み確認 (dracoLoader 設定)
- [ ] 点群 (PLY) 間引き (サンプリング率調整) 比較
- [ ] Lazy Load: 初期は軽量モデル → 詳細版遅延ロード
- [ ] テクスチャ圧縮 (KTX2) 導入 (必要ならば)
- [ ] three-mesh-bvh 導入検証 (レイキャスト高速化)
- [ ] 差分計算高速化: グリッドハッシュ or Octree 版ベンチ

### 7. デプロイ & ドキュメント
- [ ] Amplify Hosting か Vercel か選定
- [ ] ビルド設定確認 (環境変数, API エンドポイント)
- [ ] README 更新: 使用手順 / 依存関係 / モデル最適化手順
- [ ] 簡易構成図追加 (フロント <-> API <-> S3/DynamoDB)
- [ ] ライセンス / 注意事項 (LiDAR データ取扱) 記載

### 8. 拡張 / 将来機能検討
- [ ] リアルタイム同期 (AppSync Subscriptions or WebSocket)
- [ ] オフライン対応 (IndexedDB + 同期キュー)
- [ ] 差分ジョブ化 (非同期 Lambda + ステータス管理 API)
- [ ] WebXR 対応 (VR モードトグル)
- [ ] セグメンテーション: 物体タグ付け (外部モデル連携検討)

### 9. 品質 / テスト
- [ ] 単体テスト (モデルロード util, ピン座標計算)
- [ ] E2E テスト方針 (Playwright で基礎シーン表示確認)
- [ ] Lint & TypeCheck GitHub Actions ワークフロー雛形

### 10. ハッカソン当日運用チェック
- [ ] デモ用データ事前キャッシュ
- [ ] ネットワーク不調時の fallback (ローカルモデル)
- [ ] 想定質問 FAQ まとめ
- [ ] 発表スライド骨子